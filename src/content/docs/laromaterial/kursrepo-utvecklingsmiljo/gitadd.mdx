---
title: "Uppdatera fil i git repot" 
description: Vi visar hur vi uppdaterar filen README.md som en del av git repot och hur vi pushar repots ändringar till GitHub.
author: mos
revision:
    "2025-06-25": "(A, mos) Första utkastet."
sidebar:
    order: 0020
---

import Figure from '@components/CustomFigure.astro';
import YouTube from '@components/YouTube.astro';



Ditt kursrepo är ett lokalt git repo och de filer du skapar eller uppdaterar lokalt behöver du också lägga till i git repot på GitHub. Du lägger till filer via de kommandon som git erbjuder.

## git status

Börja med att kontrollera status för ditt repo med kommandot `git status`.

```console title="Kontrollera status för git repot (kursrepot)."
git status
```

Det kan se ut så här.

import git11 from '@assets/kursrepo-utvecklingsmiljo/git11.png';

<Figure 
    src={git11}
    caption="Kontrollera status på ditt repo med git status."
/>

Inget är ändrat i ditt lokala kursrepo och det är än så länge likadant som repot på GitHub.


### Uppdatera README-filen

Öppna upp filen README.md och uppdatera den. Lägg gärna till någon information om dig själv.

Det kan se ut så här.

import git12 from '@assets/kursrepo-utvecklingsmiljo/git12.png';

<Figure 
    src={git12}
    caption="Så här ser filen README.md ut innan uppdateringen."
/>


### Kontrollera git status igen

Kontrollera status i ditt lokala kursrepo igen och se att din README.md fil är uppdaterad.

Det kan se ut så här.

import git13 from '@assets/kursrepo-utvecklingsmiljo/git13.png';

<Figure 
    src={git13}
    caption="Kontrollera status på ditt repo igen med git status."
/>

Texten säger att filen `README.md` ännu inte är en del av ditt repo på GitHub. Ändringarna finns än så länge bara i ditt lokala kursrepo.  


## git add

Låt oss lägga till filen som uppdaterats med kommandot `git add`.

```console title="Lägg till den uppdaterade filen README.md till repot på GitHub."
git add README.md
```

Som ett alternativ kan du lägga till samtliga filer som ännu inte är en del av repot, enligt rapporten från `git status`.

```console title="Lägg till samtliga filer som saknas enligt git status."
git add .
```

När du lagt till filen kan du köra `git status` igen.

Det kan se ut så här.

import git2ny from '@assets/kursrepo-utvecklingsmiljo/git2ny.png';

<Figure 
    src={git2ny}
    caption="Kontrollera status på ditt repo med git status."
/>

Nu är den uppdaterade filen en del av repot, men ändringarna är ännu inte committade.

Filer som du skapar själv lägger du till på samma sätt.


## git commit

För att spara ändringarna till git repot på GitHub så kör vi kommandot `git commit` och samtidigt anger vi ett commit-meddelande om vad ändringen innebär. Dessa meddelande blir en del av repots commit-historik som berättar en historia om hur repot har utvecklats och förändrats.

```console title="Spara (samtliga) ändringarna i repot med git commit."
git commit -m "Update the file README with info about me"
```

Kör `git status` när du committat, för att se hur läget ser ut.

Det kan se ut så här.

import git3ny from '@assets/kursrepo-utvecklingsmiljo/git3ny.png';

<Figure 
    src={git3ny}
    caption="Nu är ändringarna committade till git repot."
/>

Du kan nu utläsa från `git status` att din lokala branch är ett steg före din remote origin/main som innebär att din lokala kopia har en ändring mer än den version av repot som ligger på GitHub.

När du committar bör det vara commits som du kan beskriva i ett enkelt och tydligt meddelande. Om det blir svårt att skriva ett kort meddelande om din commit så innehåller den troligen för mycket saker. Men så här i början är det viktigare att göra många commits än att skriva perfekta meddelanden till commit-historiken, det kommer med träningen.

Här är ett par korta regler när det kommer till commit-meddelanden.

1. Börja med stor bokstav
1. Sluta ej med punkt
1. Använd imperativ form, som en instruktion, exempelvis "Update..." men inte "Updated..."

## git push

För att ladda upp ändringarna till GitHub, din remote, så använder du kommandot `git push`. Då kommer samtliga lokala ändringar att laddas upp till din remote (GitHUb) så att både din remote och din lokala version är "i synk" och har samma innehåll och samma commit-historik.

Kör nu kommandot `git push`.

```console title="Ladda upp dina lokala ändringar till din remote på GitHub."
git push
```

Det kan se ut så här.

import git4ny from '@assets/kursrepo-utvecklingsmiljo/git4ny.png';

<Figure 
    src={git4ny}
    caption="Dina lokala ändringar synkas upp till din remote på GitHub."
/>

Du kan se hur utskriften från `git status` berättar att din lokala branch är i synk med din remote.



## git pull

Om läget hade varit det motsatta, att den version som ligger på GitHub är mer aktuell än din lokala kopia, då löser du det med kommandot `git pull` som laddar ned ändringarna från din remote på GitHub.

Du kan köra kommandot nu, inget kommer att hända eftersom din remote (GitHub) och din lokala version av git repot är "i synk".

```console title="Uppdatera ditt lokala repo med ändringar som är gjorde på din remote (GitHub)."
git pull
```

Det kan se ut så här.

import git5ny from '@assets/kursrepo-utvecklingsmiljo/git5ny.png';

<Figure 
    src={git5ny}
    caption="Det finns inget att hämta från din remote då du redan är i sync."
/>

Om du är osäker på att du är i synk så kan du alltid göra `git status` tillsammans med `git pull` och `git push` tills du är i sync. Ta det som en vana att alltid pusha ditt jobb till din remote, innan du lämnar din dator, så slipper du glömma det.



## git log

Den här delen är överkurs men kan vara intressant att jobba igenom.

För varje commit du gör till ett git repo så sparas det i en git commit historik, en git logg. Vi kan få fram en översikt av dessa commit-meddelanden med kommandot `git log`.

Kör följande kommando för att se hur en commit-historik kan presenteras.

```console title="Kommando för att visa commit-historik för ett git repo."
git log 
# Tryck på "q" för att avsluta utskriften, om den är lång.
```

Så här kan det till exempel se ut. Din egen utskrift kan se annorlunda ut, beroende av hur din egen historik ser ut.

import git6ny from '@assets/kursrepo-utvecklingsmiljo/git6ny.png';

<Figure 
    src={git6ny}
    caption="Utskriften från git log visar commit-historiken."
/>

vi kan förändra utskriften från kommandot genom att skicka argument till kommandot, så här.

```console title="En annorlunda formatterad utskrift av git commit historiken."
git log --oneline --graph --decorate
# Tryck på "q" för att avsluta utskriften, om den är lång.
```

Det kan se ut så här.

import git7ny from '@assets/kursrepo-utvecklingsmiljo/git7ny.png';

<Figure 
    src={git7ny}
    caption="Utskriften visar nu varje commit på en egen rad."
/>

Om du inte kommer ihåg det långa kommandot så kan du skapa ett git alias för det, så blir det lättare att skriva det i fortsättningen. Så här skapar du ett git alias för det långa kommandot, när aliaset är skapat så räcker det att skriva `git lg` för att visa historiken.

```console title="Skapa ett git alias och använda det."
# Create a git alias to view the log
git config --global alias.lg "log --oneline --graph --decorate"

# Execute the git alias
git lg
```

Nu räcker det alltså om du i fortsättningen skriver `git lg` för att se den formatterade varianten av git commit historik.

Nu i början är det kanske inte så intressant att titta i historiken, men ju mer du använder git och ju mer erfaren du blir så kan det också bli allt viktigare att ha koll på hur din commit historik ser ut.
