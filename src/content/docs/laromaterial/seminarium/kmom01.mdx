---
title: "kmom01"
description: ""
sidebar:
  order: 0110
  hidden: true
---

import { YouTube } from 'astro-embed';
import Figure from '@components/CustomFigure.astro';
import { Icon } from '@astrojs/starlight/components';

Detta seminarium tar upp följande områden: variabler, datatyper, inmatning, utmatning, villkor och selektion med if-satser.

## Förberedelser

## <Icon name="open-book" class="inline-icon" label="Interpret code" /> Teori

### Variabler, inmatning och utmatning
1. Vad är en variabel och vad händer när variabelns namn förekommer i koden?
1. Vilken av följande rader är giltiga i Python? Varför är övriga inte giltiga?
    ```python
    x + y = 2
    x*x = 2
    2 = x
    xy = 2
    ```
1. Vad är skillnaden mellan en int, en float och en str? Ge exempel.
1. Vad händer om du skriver `input()` utan att spara svaret i en variabel?
1. Vilken datatyp resulterar `input()`i?
1. Varför fungerar det inte att skriva `print(Python)`om man vill få en utskrift med texten Python.

### Villkor och selektion

1. Vad är ett **villkor**? Ge något exempel på ett villkor i vardagligt tal.
1. Ge exempel på några **jämförelseoperatorer** och vad de motsvarar i "vanligt tal"
1. Vilka av följande alternativ kan representera villkor, givet att `value`, `my_number`, `your_nr` och `answer` är variabler?:
    - `value = 45`
    - `my_number != your_nr`
    - `answer == Yes`
    - `value == "45"`
    - `answer == True`
    - `value >= 0`
1. Vad är det för språkkonstruktion som används för att styra vad som ska utföras baserat på om ett villkor är uppfyllt eller ej? Ge exempel.
1. Hur kan man uttrycka "*värdet i value är inte 100*" i python?
1. Hur kan man formulera ett villkor som blir sant om två delvillkor är uppfyllda (t.ex att förnamnet är Lisa och att födelseåret är 2002)?
1. Hur kan man formulera ett villkor som blir sant om minst ett av två villkor är uppfyllt (t.ex att förnamnet är Sven eller att efternamnet är Svensson)?
1. Hur gör man för att negera ett villkor, dvs om villkoret är falskt blir negationen sant, och vice versa?
1. Hur byggs en **if-sats** upp i python, dvs vilken är syntaxen?
1. Hur byggs en **if-else-sats** upp i python, dvs vilken är syntaxen?
1. Vad menas med **nästlade if-satser**?
1. Hur kan man skriva för att endast utföra utskriften "Jippie!" om innehållet i variabeln `value` är ett positivt jämnt tal?
1. Hur kan man kolla om en sträng är en delsträng av annan sträng. E.x. "hej" och "hej då", "hej" är en delsträng av "hej då".

### <Icon name="open-book" class="inline-icon" label="Interpret code" /> Tolka kod


1. Om du kör följande kod, vad blir det för utskrift?
    ```python
    usa_gold = 46
    uk_gold = 27
    romania_gold = 1
    total_gold = usa_gold + uk_gold + romania_gold
    print(total_gold)
    romania_gold += 1
    print(total_gold)

1. Vilka fel finns i python-koden nedan?

    ```python
    password = input("Password: ")
    if password = "tfr123":
        print("Ok")
    else:
    print("Wrong password")
    ```
1. Vad är det som inte är ok i följande kod-avsnitt?

    ```python
    tal = int(input("Skriv ett tal: "))
    if tal > 0:
        print("Talet är positivt")
    elif tal > 10:
        print("Talet är större än 10")
    else:
        print("Talet är noll eller negativt")
    ```

1. Kan man få samma resultat som i koden nedan utan att använda `else`? Om inte motivera varför? Om det är möjligt, förklara vilken ändring som behövs?

    ```python
    temperature = (int)input("What's the temperature outside?")
    if (temperature <= 0):
        print("Oh no, my water bottle will freeze!")
    else:
        print("Ok, then I can drink the water from my water bottle!")
    ```

1. Vad blir det för utskrift från koden nedan om användaren matar in `5` följt av `10`?  

    ```python
    x = int(input("Guess a number between 1 och 10: "))
    y = int(input("Guess a number between 10 och 20: "))

    if x < 7:
        if y > 15:
            print("You won!")
        else:
            print("You almost made it.")
    if y > 15:
        print("You are on the right track.")
    else:
        print("Try again.")
    ```

1. Varför är kod-avsnittet nedan inte korrekt?

    ```python ###### vill du att de ska importera random?
    nr = random.randint(-10, 10)
    if 3 / nr < 5 and nr != 0:
        print("This is nice!")
    print("Bye")

    ```

1. Vad skrivs ut och varför?

    ```python
    y = 1
    if y:
        print("Sant")
    else:
        print("Falskt")
    ```


## <Icon name="seti:python" class="inline-icon" label="Write code" /> Praktiska uppgifter

1. Ett program ska skrivas som avgör om en person får se en specifik film baserat på personens ålder och om personen har målsmans (vuxen) med sig.
    - Om filmen har åldergräns 15 år krävs medföljande målsman i det fall personen är under 15.
    - För övriga åldergränser krävs det endast att personen uppfyller ålderskravet.

    ```
    What is the movie's age rating? 15
    How old are you? 14
    Do you have an adult with you? (yes/no) yes
    Welcome, hope you enjoy the movie!
    ```

    ```
    What is the movie's age rating? 15
    How old are you? 12
    Do you have an adult with you? (yes/no) no
    Sorry, you are too young to watch the movie!
    ```

    ```
    What is the movie's age rating? 7
    How old are you? 15
    Welcome, hope you enjoy the movie!
    ```

1. Tre heltal ska matas in och medelvärdet ska beräknas. De tre talen som matas in och det beräknade medelvärdet ska skrivas ut. Dessutom ska du räkna ut vilket som är det lägsta och högsta inskrivna talet. Skriv ut om medelvärdet ligger mellan dem eller är lika med något av dem. Medelvärdet ska presenteras med 2 decimaler.
    - Ge förslag på lämpliga variabler och vilken datatyp de ska innehålla
    - Vilken/Vilka språkkonstruktion/-er behövs?
    - Vilka delproblem kan identifieras?
    - Formulera en lösning i pseudo-kod

1. Skriv ett program som avgör vilken av två inmatade strängar som är
    - längst 
    - först i alfabetisk ordning
    - innehåller bokstaven a eller A
